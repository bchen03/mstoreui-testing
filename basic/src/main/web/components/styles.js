// Styles
const Styles = (function() {

    const fullSize = {
        height:"100%", 
        width:"100%"
    };

    const fullWidth = {
        width: "100%"
    };

    const thinHr = {
        marginTop: "0",
        marginBottom: "0",
        clear: "both",
        border: "0",
        height: "1px",
        backgroundColor: "#bdbdbd"
    };

    return {
        fullSize: fullSize,
        fullWidth: fullWidth,
        thinHr: thinHr
    };
})();

export default Styles;